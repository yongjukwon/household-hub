/** Reference-aligned month-grid row height in logical phone points. */
export const CALENDAR_DAY_CELL_HEIGHT = 48

/** Matches the Calendar card inset below the month/year navigation row. */
export const CALENDAR_MONTH_HEADER_BOTTOM_SPACING = 14

/** Stable date-number surface; event dots are positioned outside this box. */
export const CALENDAR_DATE_SURFACE_SIZE = 32

/** Equal width/height and a half-size radius guarantee a circular today ring. */
export const CALENDAR_TODAY_RADIUS = CALENDAR_DATE_SURFACE_SIZE / 2
